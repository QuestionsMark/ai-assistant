export namespace Item {

    // Response
    export interface Response {
        createdAt: string;
        finished: boolean;
        id: string;
        name: string;
    }

    // export interface Element {
    //     id: string;
    //     name: string;
    // }
}